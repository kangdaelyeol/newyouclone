extends base.pug
include mixins/video

block contents
    form(method="get").search__form
        input(type="text" name="keyword" placeholder="searchingBy...")
        button Search now &rarr;
            i.fas.fa-search
    div.video-grid
        each video in videos 
            +video(video)
        else 
            span.empty__message No videos found