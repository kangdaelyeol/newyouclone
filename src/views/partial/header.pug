header 
    a(href="/").header__logo
        i.fab.fa-youtube
    nav
        ul
            li
                if login
                    li 
                        a(href="/video/upload") Upload Video
                    li 
                        a(href="/user/edit") edit profile 
                    li
                        a(href="/user/logout") logout
                    li
                        a(href=`/user/${loginUser._id}`)
                            if loginUser.avatarUrl.includes("github")
                                if loginUser.avatarUrl === ""
                                    span 😀
                                else
                                    img.header__avatar(src=loginUser.avatarUrl)
                            else
                                if loginUser.avatarUrl === ""
                                    span 😀
                                else
                                    img.header__avatar(src="/" + loginUser.avatarUrl)
                else 
                    li
                        a(href="/login") Login
                    li
                        a(href="/join").header__btn join